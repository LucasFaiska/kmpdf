# WARNING: Do not store sensitive information in this file, as its contents will be included in the Qodana report.
# Triggering CI pipeline

version: "1.0"
linter: jetbrains/qodana-jvm-android:2026.1
bootstrap: |
  ./gradlew :sharedLogic:assemble :sharedUI:assemble --no-configuration-cache --no-daemon

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - androidApp
      - .kotlin
      - build

include:
  - name: CheckDependencyLicenses

projectJDK: "17"
properties:
  gradle.ide.support.future.agp.versions: "true"
  idea.max.content.load.filesize: "20000"

coverage:
  report: build/reports/kover/report.xml
